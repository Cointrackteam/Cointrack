import React, {useState} from 'react';
import { useForm}  from "react-hook-form";

// dowload form 
export default function FormAnalysis({results}) {
    return (
        <div>
        </div>
    )
}
